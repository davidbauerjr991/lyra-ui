import{j as e}from"./jsx-runtime-D_zvdyIk.js";import{r as c}from"./index-CXOcBcs0.js";import{L as u,N as pe}from"./left-nav-CYkVGXHW.js";import{E as he}from"./empty-state-BBJWQ0ph.js";import{O as D,u as Ee,C as De}from"./create-new-outbound-mock-CcPBtd6_.js";import{I as Te}from"./interaction-nav-item-BxaJVSX7.js";import{A as ue}from"./assignments-section-caption-B0a9Cm8m.js";import{C as Ge}from"./container-Cy7Ib1VY.js";import{C as We}from"./content-area-sNeaqXFh.js";import{T as Fe}from"./tooltip-Dp368zAN.js";import{c as H}from"./utils-BLSKlp9E.js";import{I as ge}from"./inbox-IjuKb1uV.js";import{H as fe}from"./house-7dsFQekL.js";import{S as v}from"./settings-Ddbozet5.js";import{M as Le}from"./monitor-DREitwVn.js";import{L as we}from"./layout-grid-DIlLALBe.js";import{c as Be}from"./createLucideIcon-DEcfmm_F.js";import{F as G}from"./file-text-D-AW36xm.js";import{C as Re}from"./circle-help-Bj2MpUE2.js";import{G as He}from"./gauge-CgEhWZVI.js";import{C as Ue}from"./chart-column-BD5V0Ndt.js";import"./_commonjsHelpers-CqkleIqs.js";import"./tree-menu-Clac48HE.js";import"./chevron-down-BRCsRsv-.js";import"./chevron-left-C6DiQdwt.js";import"./chevron-right-DZKRY3zX.js";import"./menu-B5sfcyl8.js";import"./index-DWcjTdiE.js";import"./index-DujEZ9g2.js";import"./menu-item-CR5qklhf.js";import"./scroll-chevron-DwoFwDLx.js";import"./chevron-up-DaHnz2kU.js";import"./input-CR4qwn7s.js";import"./error-icon-solid-C6_pXXD0.js";import"./label-DTtDlf5k.js";import"./popover-CcjbzLVC.js";import"./index-BCx7cCMR.js";import"./index-De81K0_o.js";import"./index-DNfP5j1O.js";import"./floating-ui.react-dom-B7A2Lg_k.js";import"./index-DGPY9VoV.js";import"./Combination-CgjdYmp6.js";import"./tslib.es6-Ytcc2UEA.js";import"./container-header--vgxnvXX.js";import"./x-N8aIqrq2.js";import"./select-BYXQMkla.js";import"./checkbox-ExbItJLA.js";import"./index-CoT6TaLL.js";import"./minus-DYrWPnXn.js";import"./check-DrRFj5bn.js";import"./search-aUstRSOi.js";import"./radio-button-group-D2WN6WHM.js";import"./radio-DU5uc1z_.js";import"./index-ZkoUpr8J.js";import"./button-C72EbL54.js";import"./index-BDkVnVO1.js";import"./index-1evVQkiP.js";import"./badge-BsM2Tnvd.js";import"./table-Wcwa9dkO.js";import"./search-input-DYs2wuFz.js";import"./clear-button-Bldem66W.js";import"./filter-chip-BKrdhwZA.js";import"./sliders-horizontal-_yHPUfpC.js";import"./ellipsis-vertical-CZvSBcNM.js";import"./panel-left-CWVFPQ0g.js";import"./panel-right-CgZ2ABSM.js";import"./arrow-up-C-teBDU4.js";import"./favorite-button-N2jDD4ol.js";import"./star-BBKukw_S.js";import"./phone-input-D-gYLtGe.js";import"./tag-Lo5TNvOI.js";import"./plus-B2SVJpWV.js";import"./create-new-customers-data-cRxyiMHh.js";import"./phone-Di4N1bEU.js";import"./mail-CGsQAUqz.js";import"./message-square-BpbTPZlK.js";import"./channel-row-CYnewjbh.js";import"./kebab-menu-button-X2gEabCK.js";import"./menu-radix-BLTbpF2b.js";import"./index-DUC4V_Df.js";import"./tabs-RIPQUPRN.js";import"./trash-2-yAnBWR5t.js";import"./textarea-CqWH3I5r.js";import"./warning-icon-solid-C2gh2Y-U.js";import"./circle-alert-B0P0YYAI.js";import"./triangle-alert-Btkn3DL5.js";import"./circle-check-Bqo3g0Bw.js";import"./clock-xCVatdV-.js";import"./send-DPaJS59W.js";import"./user-rDz6zf5M.js";import"./actions-7Lttvy0h.js";import"./separator-CVEAaEyG.js";import"./chevrons-up-down-X1w2pWEE.js";/**
 * @license lucide-react v0.468.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */const Me=Be("PencilRuler",[["path",{d:"M13 7 8.7 2.7a2.41 2.41 0 0 0-3.4 0L2.7 5.3a2.41 2.41 0 0 0 0 3.4L7 13",key:"orapub"}],["path",{d:"m8 6 2-2",key:"115y1s"}],["path",{d:"m18 16 2-2",key:"ee94s4"}],["path",{d:"m17 11 4.3 4.3c.94.94.94 2.46 0 3.4l-2.6 2.6c-.94.94-2.46.94-3.4 0L11 17",key:"cfq27r"}],["path",{d:"M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z",key:"1a8usu"}],["path",{d:"m15 5 4 4",key:"1mk7zo"}]]);function Pe({expanded:t}){const o=e.jsxs("button",{"aria-label":"Help",className:H("flex h-9 items-center justify-center rounded-lyra-sm overflow-hidden","bg-lyra-bg-primary text-lyra-fg-on-primary transition-all duration-200","hover:bg-lyra-state-hover-primary active:bg-lyra-state-pressed-primary","focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-lyra-border-focus focus-visible:ring-offset-2",t?"w-full px-4":"w-9 px-0"),children:[e.jsx(Re,{className:"h-4 w-4 flex-shrink-0",strokeWidth:1.5,"aria-hidden":"true"}),e.jsx("span",{"aria-hidden":!t,className:H("lyra-body-md overflow-hidden whitespace-nowrap transition-all duration-200",t?"max-w-[200px] ml-2 opacity-100":"max-w-0 ml-0 opacity-0"),children:"Help"})]});return e.jsx(Fe,{content:"Help",placement:"right",disabled:t,children:e.jsx("span",{className:"flex w-full justify-center",children:o})})}function T(t){const[o,a]=t.split(":").map(i=>parseInt(i,10)||0);return o*60+a}function qe(t,o){if(o==="awaitingLongest"){const a=i=>i.awaitingResponse?T(i.elapsed):-1/0;return[...t].sort((i,f)=>a(f)-a(i))}return[...t].sort((a,i)=>T(a.elapsed)-T(i.elapsed))}const E=[{icon:e.jsx(Le,{className:"h-4 w-4",strokeWidth:1.5}),label:"Monitor"},{icon:e.jsx(we,{className:"h-4 w-4",strokeWidth:1.5}),label:"Dashboard"},{icon:e.jsx(v,{className:"h-4 w-4",strokeWidth:1.5}),label:"Configure",expandable:!0,defaultOpen:!1,children:[{label:"General"},{label:"Security"},{label:"Integrations"}]},{icon:e.jsx(Me,{className:"h-4 w-4",strokeWidth:1.5}),label:"Designer",expandable:!0,defaultOpen:!0,children:[{label:"Desktop Library",active:!0},{label:"Templates"},{label:"Components"}]},{icon:e.jsx(G,{className:"h-4 w-4",strokeWidth:1.5}),label:"Examples"},{icon:e.jsx(G,{className:"h-4 w-4",strokeWidth:1.5}),label:"Product Mockups"}],Et={title:"UI/LeftNav",component:u,tags:["autodocs"],parameters:{layout:"fullscreen"},decorators:[t=>e.jsxs("div",{className:"flex h-screen bg-lyra-bg-surface-shell",children:[e.jsx(t,{}),e.jsx(We,{children:e.jsx(Ge,{className:"flex flex-1"})})]})]},k={render:()=>{const[t,o]=c.useState(!0);return e.jsx(u,{items:E,open:t,onToggle:()=>o(a=>!a)})}},C={name:"Collapsed",render:()=>{const[t,o]=c.useState(!1);return e.jsx(u,{items:E,open:t,onToggle:()=>o(a=>!a)})}},A={name:"No Toggle Button",render:()=>e.jsx(u,{items:E,open:!0,collapsible:!1})},S={name:"Agent Next Gen Left Nav",render:()=>{const[t,o]=c.useState(!1),[a,i]=c.useState(()=>[{id:"customer-1",customerName:"Alex Miller",elapsed:"05:42",channels:[{id:"voice",type:"voice",elapsed:"05:42",current:!1,preview:"General Support"},{id:"email:alex.miller@example.com",type:"email",elapsed:"05:42",current:!0,preview:"Billing"}],channelAddresses:{voice:["+14563833329"],email:["alex.miller@example.com"]}},{id:"customer-2",customerName:"Sarah Brown",awaitingResponse:!0,elapsed:"02:15",channels:[{id:"sms:+14565559981",type:"sms",elapsed:"02:15",current:!0,preview:"Technical Support",awaitingResponse:!0,awaitingSeverity:"warning"}],channelAddresses:{sms:["+14565559981"]}},{id:"customer-3",customerName:"David Santos",elapsed:"00:39",channels:[{id:"voice",type:"voice",elapsed:"00:39",current:!0,preview:"General Support"}],channelAddresses:{voice:["+14565550147"]}}]),[f,w]=c.useState("customer-3"),[W,ye]=c.useState("lastUpdated"),[F,ve]=c.useState(!0),[xe,be]=c.useState(0),Ne=()=>{ve(n=>!n),be(n=>n+1)},L=n=>{var l;const s=(l=D.skillOptions.find(m=>m.value===n.skillId))==null?void 0:l.label,r={id:`${n.channel}:${n.phone}`,type:n.channel,elapsed:"00:00",current:!0,preview:s};i(m=>{const p=m.findIndex(d=>d.id===n.contact.id);return p===-1?[...m,{id:n.contact.id,customerName:n.contact.name,elapsed:"00:00",channels:[r],channelAddresses:{[n.channel]:[n.phone]}}]:m.map((d,h)=>{var R;if(h!==p)return d;const x=d.channels.findIndex(N=>(N.id??N.type)===r.id),b=x===-1?[...d.channels,r]:d.channels.map((N,je)=>je===x?r:N),g=((R=d.channelAddresses)==null?void 0:R[n.channel])??[];return{...d,elapsed:"00:00",channels:b,channelAddresses:{...d.channelAddresses,[n.channel]:g.includes(n.phone)?g:[...g,n.phone]}}})}),w(n.contact.id),o(!0)},ke=n=>{const s=`quickdial:${n}`,r={id:"voice",type:"voice",elapsed:"00:00",current:!0};i(l=>{const m=l.findIndex(p=>p.id===s);return m===-1?[...l,{id:s,elapsed:"00:00",channels:[r]}]:l.map((p,d)=>d===m?{...p,elapsed:"00:00",channels:[r]}:p)}),w(s),o(!0)},Ce=n=>{i(s=>s.filter(r=>r.id!==n)),w(s=>s===n?"":s)},Ae=(n,s)=>{var p;const r=s.id??s.type,l=`${s.type}:`,m=(p=s.id)!=null&&p.startsWith(l)?s.id.slice(l.length):void 0;i(d=>d.map(h=>{var b;if(h.id!==n)return h;const x=m?{...h.channelAddresses,[s.type]:(((b=h.channelAddresses)==null?void 0:b[s.type])??[]).filter(g=>g!==m)}:h.channelAddresses;return{...h,channels:h.channels.filter(g=>(g.id??g.type)!==r),channelAddresses:x}}))},B=c.useMemo(()=>{const n=new Map(a.filter(s=>s.channelAddresses).map(s=>[s.id,s.channelAddresses]));return{...D,groups:D.groups.map(s=>s.contacts?{...s,contacts:s.contacts.map(r=>{const l=n.get(r.id);return!l||Object.keys(l).length===0?r:{...r,openChannelAddresses:l}})}:s)}},[a]),{getHeaderAction:Se}=Ee({...B,onStartCall:L}),Ie={icon:e.jsx(fe,{className:"h-4 w-4",strokeWidth:1.5}),label:"Desk",active:f==="",onClick:()=>w("")},Oe={icon:e.jsx(v,{className:"h-4 w-4",strokeWidth:1.5}),label:"Settings"};return e.jsx(u,{items:[Ie],open:t,onToggle:()=>o(n=>!n),itemsFirst:!0,headerFillsHeight:!0,stickyCaption:e.jsx(ue,{expanded:t,count:a.length,sort:W,onSortChange:ye,allExpanded:F,onToggleAllExpanded:Ne,compact:!0}),footer:e.jsx(pe,{expanded:t,items:[Oe]}),pinnedHeader:e.jsx(De,{title:"New Outbound",outbound:{...B,onStartCall:L,onQuickDial:ke},expanded:t}),header:e.jsxs(e.Fragment,{children:[a.length===0&&t&&e.jsx("div",{className:"flex flex-1 min-h-0 items-center justify-center animate-in fade-in-0 duration-150 delay-200 fill-mode-backwards",children:e.jsx(he,{icon:e.jsx(ge,{className:"h-8 w-8",strokeWidth:1.5}),message:"Your assignment queue is empty",description:"New assignments will appear here.",className:"h-auto w-full py-6",tone:"secondary"})}),qe(a,W).map(n=>e.jsx(Te,{customerName:n.customerName,active:f===n.id,onClick:()=>w(n.id),awaitingResponse:n.awaitingResponse,elapsed:n.elapsed,expanded:t,channels:n.channels,onDismiss:()=>Ce(n.id),onDismissChannel:s=>Ae(n.id,s),headerAction:Se(n.id),collapsible:!0,channelsExpandedOverride:{expanded:F,version:xe}},n.id))]})})}},y={name:"Empty State",render:()=>{const[t,o]=c.useState(!0),a={icon:e.jsx(fe,{className:"h-4 w-4",strokeWidth:1.5}),label:"Desk",active:!0},i={icon:e.jsx(v,{className:"h-4 w-4",strokeWidth:1.5}),label:"Settings"};return e.jsx(u,{items:[a],open:t,onToggle:()=>o(f=>!f),itemsFirst:!0,headerFillsHeight:!0,stickyCaption:e.jsx(ue,{expanded:t,count:0,sort:"lastUpdated",onSortChange:()=>{},allExpanded:!0,onToggleAllExpanded:()=>{},compact:!0}),footer:e.jsx(pe,{expanded:t,items:[i]}),header:e.jsx(e.Fragment,{children:t&&e.jsx("div",{className:"flex flex-1 min-h-0 items-center justify-center animate-in fade-in-0 duration-150 delay-200 fill-mode-backwards",children:e.jsx(he,{icon:e.jsx(ge,{className:"h-8 w-8",strokeWidth:1.5}),message:"Your assignment queue is empty",description:"New assignments will appear here.",className:"h-auto w-full py-6",tone:"secondary"})})})})}},I={name:"Outbound Engagement Left Nav",render:()=>{const[t,o]=c.useState(!0),a=[{icon:e.jsx(He,{className:"h-4 w-4",strokeWidth:1.5}),label:"Monitor",active:!0},{icon:e.jsx(v,{className:"h-4 w-4",strokeWidth:1.5}),label:"Configure"},{icon:e.jsx(Ue,{className:"h-4 w-4",strokeWidth:1.5}),label:"Review"}];return e.jsx(u,{items:a,open:t,onToggle:()=>o(i=>!i)})}},O={name:"Footer Button (Expandable)",render:()=>{const[t,o]=c.useState(!0);return e.jsx(u,{items:E,open:t,onToggle:()=>o(a=>!a),footer:e.jsx(Pe,{expanded:t})})}},j={name:"Custom Items",render:()=>{const[t,o]=c.useState(!0),a=[{icon:e.jsx(we,{className:"h-4 w-4",strokeWidth:1.5}),label:"Overview",active:!0},{icon:e.jsx(v,{className:"h-4 w-4",strokeWidth:1.5}),label:"Settings",expandable:!0,defaultOpen:!0,children:[{label:"Profile"},{label:"Notifications",active:!0},{label:"Privacy"}]},{icon:e.jsx(G,{className:"h-4 w-4",strokeWidth:1.5}),label:"Reports"}];return e.jsx(u,{items:a,open:t,onToggle:()=>o(i=>!i)})}};var U,M,P;k.parameters={...k.parameters,docs:{...(U=k.parameters)==null?void 0:U.docs,source:{originalSource:`{
  render: () => {
    const [open, setOpen] = useState(true);
    return <LeftNav items={sampleItems} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(P=(M=k.parameters)==null?void 0:M.docs)==null?void 0:P.source}}};var q,_,$;C.parameters={...C.parameters,docs:{...(q=C.parameters)==null?void 0:q.docs,source:{originalSource:`{
  name: "Collapsed",
  render: () => {
    const [open, setOpen] = useState(false);
    return <LeftNav items={sampleItems} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...($=(_=C.parameters)==null?void 0:_.docs)==null?void 0:$.source}}};var V,Q,Y;A.parameters={...A.parameters,docs:{...(V=A.parameters)==null?void 0:V.docs,source:{originalSource:`{
  name: "No Toggle Button",
  render: () => <LeftNav items={sampleItems} open={true} collapsible={false} />
}`,...(Y=(Q=A.parameters)==null?void 0:Q.docs)==null?void 0:Y.source}}};var z,K,J;S.parameters={...S.parameters,docs:{...(z=S.parameters)==null?void 0:z.docs,source:{originalSource:`{
  name: "Agent Next Gen Left Nav",
  render: () => {
    const [open, setOpen] = useState(false);

    // Seeded with 3 real contacts from \`OUTBOUND_CONFIG\`'s own "customers"
    // group (ids "customer-1"/"customer-2"/"customer-3", see
    // create-new-customers-data.ts) rather than left empty — per explicit
    // request, this story should show what multiple concurrent assignments
    // actually look like (single- vs. multi-channel cards, a card awaiting
    // response) instead of only being reachable by manually clicking through
    // CreateNew's "New Outbound" flow first. Every id/channel still maps to
    // a real \`OUTBOUND_CONFIG\` contact — same "no fake ids with no backing
    // contact" reasoning the old empty-start comment (now below, on
    // \`handleStartCall\`) already documents, so each seeded card's own "+"
    // button works exactly like one actually started here would.
    //
    // Ordered oldest → newest (array order is render order, same as every
    // card \`handleStartCall\` appends below) with David Santos' card last —
    // the most-recently-started assignment — and its id is what \`activeId\`
    // below initializes to, so the newest assignment is the one shown
    // selected/highlighted on load, matching what already happens the
    // moment a real "Start Interaction" click lands (\`setActiveId(selection
    // .contact.id)\` in \`handleStartCall\`).
    const [interactions, setInteractions] = useState<AgentNextGenDemoInteraction[]>(() => [{
      id: "customer-1",
      customerName: "Alex Miller",
      elapsed: "05:42",
      channels: [{
        id: "voice",
        type: "voice",
        elapsed: "05:42",
        current: false,
        preview: "General Support"
      }, {
        id: "email:alex.miller@example.com",
        type: "email",
        elapsed: "05:42",
        current: true,
        preview: "Billing"
      }],
      channelAddresses: {
        voice: ["+14563833329"],
        email: ["alex.miller@example.com"]
      }
    }, {
      id: "customer-2",
      customerName: "Sarah Brown",
      awaitingResponse: true,
      elapsed: "02:15",
      channels: [{
        id: "sms:+14565559981",
        type: "sms",
        elapsed: "02:15",
        current: true,
        preview: "Technical Support",
        awaitingResponse: true,
        awaitingSeverity: "warning"
      }],
      channelAddresses: {
        sms: ["+14565559981"]
      }
    }, {
      id: "customer-3",
      customerName: "David Santos",
      elapsed: "00:39",
      channels: [{
        id: "voice",
        type: "voice",
        elapsed: "00:39",
        current: true,
        preview: "General Support"
      }],
      channelAddresses: {
        voice: ["+14565550147"]
      }
    }]);
    // Click any InteractionNavItem card below to make it the active one —
    // lets you test the active/inactive + awaiting-response border and
    // highlight states interactively instead of only via fixed args.
    // Starts on "customer-3" — the latest (most recently started, per the
    // seed list above) of the three seeded assignments — rather than "",
    // so the newest card is the one shown active on load.
    const [activeId, setActiveId] = useState("customer-3");

    // Drives \`AssignmentsSectionCaption\`'s sort button + collapse-all — see
    // assignments-section-caption.tsx's own doc comments. \`interactions\`
    // itself stays in insertion order for everything else that reads it;
    // only the render below reorders a copy (\`sortAssignments\`).
    const [assignmentSort, setAssignmentSort] = useState<AssignmentSortValue>("lastUpdated");
    const [channelsAllExpanded, setChannelsAllExpanded] = useState(true);
    const [channelsExpandedOverrideVersion, setChannelsExpandedOverrideVersion] = useState(0);
    const handleToggleAllChannelsExpanded = () => {
      setChannelsAllExpanded(v => !v);
      setChannelsExpandedOverrideVersion(v => v + 1);
    };

    // A NEWLY started interaction (from here on, via CreateNew's "New
    // Outbound" flow — see handleStartCall/handleQuickDial below) still
    // gets a real backing contact the same way the 3 seeded cards above do:
    // this story used to seed 3 fixed placeholder cards ("Sofia Martinez"/
    // "Ray Torres"/"Customer") with ids that had no matching contact in
    // \`OUTBOUND_CONFIG\` — clicking their "+" could never actually open
    // CreateNew's call-setup screen, which looked like a bug in the "+"
    // button itself rather than what it actually was (cards with no real
    // contact behind them). Only populating via a real "Start Interaction"
    // click (on top of the 3 seeded, likewise-real cards above) means every
    // card that ever appears here is backed by a real contact, so its own
    // "+" button always works.
    // Starting a new outbound call/quick dial adds (or restarts) a card —
    // and, since a collapsed nav would otherwise hide that new card from
    // view entirely, also expands the nav so the agent immediately sees
    // what they just started instead of having to notice and open it
    // themselves.
    const handleStartCall = (selection: {
      contact: CreateNewOutboundContact;
      channel: ChannelType;
      phone: string;
      skillId: string;
    }) => {
      // Body copy shows the *actual* skill the agent picked in CreateNew's
      // outbound flow — not a random one — so the card reads as "here's
      // what this call was routed under," matching what the agent just
      // chose. \`selection.skillId\` is the value from \`OUTBOUND_CONFIG.
      // skillOptions\`; resolve it back to that option's own label.
      const skillLabel = OUTBOUND_CONFIG.skillOptions.find(o => o.value === selection.skillId)?.label;
      const newChannel: InteractionChannel = {
        id: \`\${selection.channel}:\${selection.phone}\`,
        type: selection.channel,
        elapsed: "00:00",
        current: true,
        preview: skillLabel
      };
      setInteractions(prev => {
        const idx = prev.findIndex(i => i.id === selection.contact.id);
        // No existing card for this contact — start a new one.
        if (idx === -1) {
          return [...prev, {
            id: selection.contact.id,
            customerName: selection.contact.name,
            elapsed: "00:00",
            channels: [newChannel],
            channelAddresses: {
              [selection.channel]: [selection.phone]
            }
          }];
        }
        // Same contact already has a card open — restart the matching
        // channel if this is the *same* type+address (e.g. redialing the
        // same SMS number), or add a new row alongside the existing ones if
        // it's a different address on the same type (e.g. a second SMS
        // thread on a different number) — those are genuinely separate
        // conversations, not a duplicate of the first, so they shouldn't
        // overwrite it.
        return prev.map((interaction, i) => {
          if (i !== idx) return interaction;
          const chIdx = interaction.channels.findIndex(c => (c.id ?? c.type) === newChannel.id);
          const channels = chIdx === -1 ? [...interaction.channels, newChannel] : interaction.channels.map((c, j) => j === chIdx ? newChannel : c);
          const existingAddresses = interaction.channelAddresses?.[selection.channel] ?? [];
          return {
            ...interaction,
            elapsed: "00:00",
            channels,
            channelAddresses: {
              ...interaction.channelAddresses,
              [selection.channel]: existingAddresses.includes(selection.phone) ? existingAddresses : [...existingAddresses, selection.phone]
            }
          };
        });
      });
      setActiveId(selection.contact.id);
      setOpen(true);
    };
    const handleQuickDial = (phoneNumber: string) => {
      // No contact record for a quick-dialed number — key the card off the
      // number itself so redialing it restarts the existing card rather
      // than stacking up duplicates.
      const id = \`quickdial:\${phoneNumber}\`;
      const newChannel: InteractionChannel = {
        id: "voice",
        type: "voice",
        elapsed: "00:00",
        current: true
      };
      setInteractions(prev => {
        const idx = prev.findIndex(i => i.id === id);
        if (idx === -1) return [...prev, {
          id,
          elapsed: "00:00",
          channels: [newChannel]
        }];
        return prev.map((interaction, i) => i === idx ? {
          ...interaction,
          elapsed: "00:00",
          channels: [newChannel]
        } : interaction);
      });
      setActiveId(id);
      setOpen(true);
    };

    // "Unassign & Dismiss" — InteractionNavItem itself decides which of
    // these two applies (based on how many channels the card has open when
    // it's clicked): \`onDismiss\` (whole card, only called when just one
    // channel was open) removes the interaction entirely, clearing
    // \`activeId\` too if it was the active one, same as the live app's
    // \`handleDismissInteraction\`; \`onDismissChannel\` (only called when more
    // than one channel was open) drops just that one channel.
    const handleDismissInteraction = (id: string) => {
      setInteractions(prev => prev.filter(interaction => interaction.id !== id));
      setActiveId(current => current === id ? "" : current);
    };
    const handleDismissChannel = (id: string, channel: InteractionChannel) => {
      // Match on \`id\` (falling back to \`type\`, same as InteractionNavItem's
      // own \`channelKey\` convention) — two open channels can share a \`type\`
      // (e.g. two SMS threads on different numbers), so filtering by \`type\`
      // alone would drop *both* instead of just the one dismissed.
      const dismissedKey = channel.id ?? channel.type;
      // \`id\` is built as \`\${type}:\${address}\` in handleStartCall — recover
      // the address so only *that* entry (not every address for this type)
      // is dropped from \`channelAddresses\` too, otherwise a still-open
      // second SMS thread would wrongly stop disabling its own address in
      // CreateNew once this one is dismissed.
      const addressPrefix = \`\${channel.type}:\`;
      const dismissedAddress = channel.id?.startsWith(addressPrefix) ? channel.id.slice(addressPrefix.length) : undefined;
      setInteractions(prev => prev.map(interaction => {
        if (interaction.id !== id) return interaction;
        const remainingAddresses = dismissedAddress ? {
          ...interaction.channelAddresses,
          [channel.type]: (interaction.channelAddresses?.[channel.type] ?? []).filter(a => a !== dismissedAddress)
        } : interaction.channelAddresses;
        return {
          ...interaction,
          channels: interaction.channels.filter(c => (c.id ?? c.type) !== dismissedKey),
          channelAddresses: remainingAddresses
        };
      }));
    };

    // A contact already reachable via a currently-open channel still shows
    // that channel in "Select Channel" and every address in the detail
    // screen's second field — except the one exact address already in use,
    // which is disabled. See AgentNextGenTemplate.stories.tsx's own copy of
    // this comment for the full rationale. Recomputed whenever
    // \`interactions\` changes so an address re-enables once its card is
    // dismissed.
    const outboundConfig = useMemo<CreateNewOutboundConfig>(() => {
      const openAddressesByContactId = new Map<string, Partial<Record<ChannelType, string[]>>>(interactions.filter(interaction => interaction.channelAddresses).map(interaction => [interaction.id, interaction.channelAddresses as Partial<Record<ChannelType, string[]>>]));
      return {
        ...OUTBOUND_CONFIG,
        groups: OUTBOUND_CONFIG.groups.map(group => {
          if (!group.contacts) return group;
          return {
            ...group,
            contacts: group.contacts.map(contact => {
              const openChannelAddresses = openAddressesByContactId.get(contact.id);
              if (!openChannelAddresses || Object.keys(openChannelAddresses).length === 0) return contact;
              return {
                ...contact,
                openChannelAddresses
              };
            })
          };
        })
      };
    }, [interactions]);

    // Every "Agent Next Gen" consumer (this story, AgentNextGenTemplate.
    // stories.tsx, agent-next-gen-v1's AgentNextGenPage.tsx) wants the exact
    // same "+" behavior on each InteractionNavItem card — look up that
    // interaction's underlying outbound contact, scope the flyout to
    // whatever channels it actually supports, and render a fully
    // self-contained popover (channel picker + detail form) right where the
    // "+" was clicked. That's \`useOutboundAddButton\` (create-new.tsx) — a
    // single shared implementation instead of three hand-copied ones that
    // could (and did) quietly drift out of sync. \`OutboundAddButton\` no
    // longer hands off to a separate, remotely-anchored \`CreateNew\`
    // instance via \`launchRequest\`, so there's nothing to destructure here
    // beyond \`getHeaderAction\`.
    // \`onStartCall: handleStartCall\` override is required — \`outboundConfig\`
    // itself still carries whatever placeholder \`onStartCall\` it was built
    // with (see its own \`useMemo\` above), not this story's real
    // \`handleStartCall\`, so the bare \`outboundConfig\` here would silently
    // no-op "Start Interaction" instead of opening a card (same bug fixed
    // in agent-next-gen-v1/AgentNextGenPage.tsx and
    // AgentNextGenTemplate.stories.tsx — see either one's own comment on
    // this line).
    const {
      getHeaderAction
    } = useOutboundAddButton({
      ...outboundConfig,
      onStartCall: handleStartCall
    });

    // "Desk" is this page itself (see AgentDashboard/\`Templates/Dashboards\`'
    // PageHeader title) — active by default, and the only item here with no
    // fixed \`active\` flag: it's derived so that starting/selecting a real
    // interaction (activeId set) takes the active state away from Desk,
    // since focus has moved to that assignment instead. \`onClick\` clears
    // \`activeId\` back to "" so Desk stays a real way back to this page
    // instead of just a static "you're on the dashboard" indicator.
    //
    // Split into two separate items (not one combined \`items\` array
    // anymore) to match \`agent-next-gen-v2\`'s own restructuring: "Desk"
    // renders above the Assignments caption/cards via \`itemsFirst\`,
    // "Settings" renders genuinely pinned to the true bottom via \`footer\`
    // + \`NavRail\` — see the \`<LeftNav>\` call below for the full reasoning
    // (kept inline there, matching \`AgentNextGenPage.tsx\`'s own comments).
    const homeItem: NavItem = {
      icon: <Home className="h-4 w-4" strokeWidth={1.5} />,
      label: "Desk",
      active: activeId === "",
      onClick: () => setActiveId("")
    };
    const settingsItem: NavItem = {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Settings"
    };
    return <LeftNav items={[homeItem]} open={open} onToggle={() => setOpen(v => !v)}
    // \`itemsFirst\` — "Desk" renders ABOVE \`header\` (the "Assignments"
    // caption + empty-state/cards), \`sticky top-0\`, directly under
    // \`pinnedHeader\` ("New Outbound"). "Settings" goes to \`footer\`
    // instead (below), genuinely pinned to the true bottom of the rail
    // rather than sharing this sticky-top spot with "Desk".
    itemsFirst
    // Lets \`header\` grow to fill whatever height "Desk"+"New Outbound"
    // don't use, so the "Your assignment queue is empty" message below
    // can be vertically centered in that leftover space — see its own
    // centering wrapper, and \`headerFillsHeight\`'s doc comment in
    // left-nav.tsx.
    headerFillsHeight
    // "Assignments (N)" caption — per explicit follow-up request ("fix
    // the assignments header under the home button so it doesn't
    // scroll"), mirrored from \`agent-next-gen-v2\` (see
    // \`AgentNextGenPage.tsx\`'s own comment on this same prop for the
    // fuller writeup): rendered via \`stickyCaption\` now, the same
    // sticky-top box as \`items\` ("Desk"), so it stays pinned instead of
    // scrolling away with the cards under it.
    stickyCaption={<AssignmentsSectionCaption expanded={open} count={interactions.length} sort={assignmentSort} onSortChange={setAssignmentSort} allExpanded={channelsAllExpanded} onToggleAllExpanded={handleToggleAllChannelsExpanded} compact />}
    // "Settings" — genuinely pinned to the TRUE bottom of the nav (not
    // just \`sticky bottom-0\`, which only holds once the card list
    // actually overflows). \`footer\` renders as a sibling AFTER the
    // whole scrollable region — always at the aside's real bottom edge
    // regardless of content above it. \`NavRail\` renders this single
    // item with the exact same TreeMenu/icon-only styling \`items\`
    // itself uses. \`expanded={open}\` passed explicitly, not left to
    // \`injectExpanded\` — \`left-nav.tsx\`'s inline (non-overlay) mode
    // renders \`footer\` as \`{footer}\` directly with no injection at
    // all (only the overlay-mode branch auto-injects \`expanded\`).
    footer={<NavRail expanded={open} items={[settingsItem]} />} pinnedHeader={<CreateNew title="New Outbound" outbound={{
      ...outboundConfig,
      onStartCall: handleStartCall,
      onQuickDial: handleQuickDial
    }} expanded={open} />} header={<>
            {/* "Assignments (N active)" caption + collapse-all/sort — now
                rendered via \`stickyCaption\` above (pinned with "Desk"
                instead of scrolling with these cards), not here. See
                assignments-section-caption.tsx's own doc comment for the
                caption's own reasoning; \`count\` there is still
                \`interactions.length\`, the exact same live list these
                cards render from, so the two numbers can't drift apart. */}
            {/* Dismiss every seeded/started card (each card's own "×") to
                see this live — matches \`agent-next-gen-v2\`'s own empty
                state exactly, including the vertical centering
                (\`headerFillsHeight\` above), the \`tone="secondary"\` color
                match to Contact History's "Nothing to Display" placeholder,
                and the delayed fade-in below (see the "Empty State" story,
                further down this file, for a dedicated demo that doesn't
                require dismissing cards first).
                 \`animate-in fade-in-0 duration-150 delay-200
                fill-mode-backwards\` — per explicit follow-up ("I can see
                the text growing/shrinking ... have it fade in"), mirrored
                from \`agent-next-gen-v2\`'s own \`AgentNextGenPage.tsx\` (see
                that file's comment on this same block for the full
                writeup): this message mounts the instant \`open\` flips
                true, while the \`<aside>\` itself (left-nav.tsx) is still
                mid-\`transition-all duration-200\` on its own width. Without
                the delay, the message sits inside a still-widening box for
                that whole 200ms and visibly rewraps as more room opens up.
                \`delay-200\` matches that width transition's own duration so
                the fade only starts once it's settled; \`fill-mode-
                backwards\` holds the message at opacity 0 for the entire
                delay (approximating the requested \`display: none\` →
                \`display: inline\`, since \`display\` itself can't be
                transitioned). No equivalent needed on close — this block
                unmounts instantly when \`open\` goes false, before the aside
                starts collapsing. */}
            {interactions.length === 0 && open && <div className="flex flex-1 min-h-0 items-center justify-center animate-in fade-in-0 duration-150 delay-200 fill-mode-backwards">
                <EmptyState icon={<Inbox className="h-8 w-8" strokeWidth={1.5} />} message="Your assignment queue is empty" description="New assignments will appear here." className="h-auto w-full py-6" tone="secondary" />
              </div>}
            {sortAssignments(interactions, assignmentSort).map(interaction => <InteractionNavItem key={interaction.id} customerName={interaction.customerName} active={activeId === interaction.id} onClick={() => setActiveId(interaction.id)} awaitingResponse={interaction.awaitingResponse} elapsed={interaction.elapsed} expanded={open} channels={interaction.channels} onDismiss={() => handleDismissInteraction(interaction.id)} onDismissChannel={channel => handleDismissChannel(interaction.id, channel)}
      // Per v2: the expanded card's "+" (Add Channel,
      // \`getHeaderAction\` below) is replaced with a chevron that
      // expands/collapses this card's own channel list —
      // \`headerAction\` itself is left wired below (harmless;
      // \`InteractionNavItem\` ignores it whenever \`collapsible\`
      // is true) rather than removed.
      headerAction={getHeaderAction(interaction.id)} collapsible
      // "Collapse all"/"Expand all" — see \`channelsAllExpanded\`'s
      // own doc comment near its declaration for why this is a
      // one-shot \`{ expanded, version }\` override object rather
      // than a plain controlled boolean.
      channelsExpandedOverride={{
        expanded: channelsAllExpanded,
        version: channelsExpandedOverrideVersion
      }} />)}
          </>} />;
  }
}`,...(J=(K=S.parameters)==null?void 0:K.docs)==null?void 0:J.source}}};var X,Z,ee,ne,te;y.parameters={...y.parameters,docs:{...(X=y.parameters)==null?void 0:X.docs,source:{originalSource:`{
  name: "Empty State",
  render: () => {
    const [open, setOpen] = useState(true);
    const homeItem: NavItem = {
      icon: <Home className="h-4 w-4" strokeWidth={1.5} />,
      label: "Desk",
      active: true
    };
    const settingsItem: NavItem = {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Settings"
    };
    return <LeftNav items={[homeItem]} open={open} onToggle={() => setOpen(v => !v)} itemsFirst headerFillsHeight
    // \`count={0}\` — always empty, so \`AssignmentsSectionCaption\` never
    // shows its sort/collapse-all actions (\`showActions = count > 1\`);
    // the sort/toggle props below are still required by its own type
    // but are never actually invoked here. Rendered via \`stickyCaption\`
    // (pinned with "Desk" instead of scrolling), matching the real app
    // and the \`AgentNextGen\` story above.
    stickyCaption={<AssignmentsSectionCaption expanded={open} count={0} sort="lastUpdated" onSortChange={() => {}} allExpanded onToggleAllExpanded={() => {}} compact />} footer={<NavRail expanded={open} items={[settingsItem]} />} header={<>
            {open && <div className="flex flex-1 min-h-0 items-center justify-center animate-in fade-in-0 duration-150 delay-200 fill-mode-backwards">
                <EmptyState icon={<Inbox className="h-8 w-8" strokeWidth={1.5} />} message="Your assignment queue is empty" description="New assignments will appear here." className="h-auto w-full py-6" tone="secondary" />
              </div>}
          </>} />;
  }
}`,...(ee=(Z=y.parameters)==null?void 0:Z.docs)==null?void 0:ee.source},description:{story:`Dedicated demo of the left nav's "no assignments" empty state — per
explicit request ("add a story to the left nav that shows an empty state
like this"), so it's visible directly on load instead of only being
reachable by manually dismissing every seeded/started card in the
\`AgentNextGen\` story above.

Deliberately minimal: a real \`homeItem\`/\`footer\` (\`NavRail\` + Settings,
same as \`AgentNextGen\`) so the empty state reads in its real context —
still inside the actual rail chrome, not floating in isolation — but no
\`pinnedHeader\`/\`CreateNew\` "New Outbound" trigger and no interaction
seeding/dismissal logic at all, since this story has exactly one state to
show and nothing here should ever add a card to it. \`count={0}\` is a
fixed prop, not local state driven by any list.

Toggle the collapse/expand button (or \`Collapsed\`'s own control) to watch
the message mount fresh each time the rail re-opens — the delayed
\`animate-in fade-in-0 duration-150 delay-200 fill-mode-backwards\` on its
wrapper (mirrored from \`agent-next-gen-v2\`'s own fix; see
\`AgentNextGenPage.tsx\`'s comment on this same block for the full
writeup) is exactly what this story exists to demonstrate: the message
stays invisible through the rail's own 200ms width transition, then
fades in once that settles, instead of visibly rewrapping as the rail
widens.`,...(te=(ne=y.parameters)==null?void 0:ne.docs)==null?void 0:te.description}}};var se,ae,oe;I.parameters={...I.parameters,docs:{...(se=I.parameters)==null?void 0:se.docs,source:{originalSource:`{
  name: "Outbound Engagement Left Nav",
  render: () => {
    const [open, setOpen] = useState(true);
    const items: NavItem[] = [{
      icon: <Gauge className="h-4 w-4" strokeWidth={1.5} />,
      label: "Monitor",
      active: true
    }, {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Configure"
    }, {
      icon: <BarChart3 className="h-4 w-4" strokeWidth={1.5} />,
      label: "Review"
    }];
    return <LeftNav items={items} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(oe=(ae=I.parameters)==null?void 0:ae.docs)==null?void 0:oe.source}}};var ie,re,le;O.parameters={...O.parameters,docs:{...(ie=O.parameters)==null?void 0:ie.docs,source:{originalSource:`{
  name: "Footer Button (Expandable)",
  render: () => {
    const [open, setOpen] = useState(true);
    return <LeftNav items={sampleItems} open={open} onToggle={() => setOpen(v => !v)} footer={<FooterButton expanded={open} />} />;
  }
}`,...(le=(re=O.parameters)==null?void 0:re.docs)==null?void 0:le.source}}};var de,ce,me;j.parameters={...j.parameters,docs:{...(de=j.parameters)==null?void 0:de.docs,source:{originalSource:`{
  name: "Custom Items",
  render: () => {
    const [open, setOpen] = useState(true);
    const items: NavItem[] = [{
      icon: <LayoutGrid className="h-4 w-4" strokeWidth={1.5} />,
      label: "Overview",
      active: true
    }, {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Settings",
      expandable: true,
      defaultOpen: true,
      children: [{
        label: "Profile"
      }, {
        label: "Notifications",
        active: true
      }, {
        label: "Privacy"
      }]
    }, {
      icon: <FileText className="h-4 w-4" strokeWidth={1.5} />,
      label: "Reports"
    }];
    return <LeftNav items={items} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(me=(ce=j.parameters)==null?void 0:ce.docs)==null?void 0:me.source}}};const Dt=["Default","Collapsed","NoToggle","AgentNextGen","EmptyStateExample","OutboundEngagement","FooterButtonExample","CustomItems"];export{S as AgentNextGen,C as Collapsed,j as CustomItems,k as Default,y as EmptyStateExample,O as FooterButtonExample,A as NoToggle,I as OutboundEngagement,Dt as __namedExportsOrder,Et as default};
