import{j as e}from"./jsx-runtime-D_zvdyIk.js";import{r as x}from"./index-DhMLlvMY.js";import{c as T}from"./utils-BLSKlp9E.js";import{M}from"./menu-radix-BHyM5alN.js";import{B as k}from"./badge-CJVmnMhy.js";import{E as N}from"./ellipsis-vertical-D6ttVBVO.js";const c=x.forwardRef(({items:h,ariaLabel:a,className:u,as:g="button",icon:m,align:f="right",onOpenChange:b,badge:n,onClick:o,disabled:t,open:w,...r},v)=>{const i=T("flex h-6 w-6 shrink-0 items-center justify-center rounded-lyra-sm text-lyra-fg-secondary transition-colors hover:bg-lyra-state-hover focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-lyra-border-focus",t&&"pointer-events-none opacity-40 hover:bg-transparent",u),s=m??e.jsx(N,{className:"h-3.5 w-3.5",strokeWidth:1.5,"aria-hidden":"true"}),l=n!=null&&n>0?e.jsxs("span",{className:"relative inline-flex",children:[e.jsx("span",{"aria-hidden":"true",children:s}),e.jsx(k,{shape:"circle",variant:"critical",size:"sm",count:n,className:"absolute -top-2 -right-2"})]}):s,d=p=>{t||(o==null||o(p),p.stopPropagation())},y=g==="span"?e.jsx("span",{role:"button",tabIndex:t?-1:0,"aria-disabled":t||void 0,"aria-label":a,className:i,...r,onClick:d,children:l}):e.jsx("button",{type:"button","aria-label":a,disabled:t,className:i,...r,onClick:d,children:l});return e.jsx(M,{ref:v,trigger:y,items:h,align:f==="left"?"start":"end",sideOffset:4,open:w,onOpenChange:b})});c.displayName="KebabMenuButton";c.__docgenInfo={description:"",methods:[],displayName:"KebabMenuButton",props:{onClick:{required:!1,tsType:{name:"ReactMouseEventHandler",raw:"React.MouseEventHandler<HTMLElement>",elements:[{name:"HTMLElement"}]},description:"Extends `HTMLAttributes` (minus `children`/`className`/`onClick`, which\nthis component already owns) so this trigger can be wrapped directly in\na `<Tooltip>` — e.g. `channel-row.tsx`'s \"More Options\" tooltip on this\nexact kebab. `Tooltip`'s `TooltipPrimitive.Trigger asChild` clones\nwhatever single child it wraps and injects its own hover/focus tracking\nprops (`onPointerEnter`/`onPointerLeave`/`onFocus`/`onBlur`, etc.) onto\nit — that works automatically for a plain DOM element (React merges\ncloned props directly onto it), but NOT for a custom component like\nthis one unless it explicitly accepts and forwards those extra props\nitself. Before this, wrapping `<KebabMenuButton>` in `<Tooltip>` silently\ndid nothing on hover: the injected props landed on this component's own\nprop object, where they were destructured away and dropped (this\nfunction only ever read its own known prop names), never reaching the\nactual `<button>`/`<span>` Radix needs to attach its hover listeners to.\n`onClick` is still handled separately below (not spread generically)\nsince it has to be composed with this component's own `stopPropagation`\ncall rather than just overwritten either direction."},items:{required:!0,tsType:{name:"Array",elements:[{name:"union",raw:'MenuItemDef | "separator" | MenuSectionLabel',elements:[{name:"MenuItemDef"},{name:"literal",value:'"separator"'},{name:"MenuSectionLabel"}]}],raw:"MenuEntry[]"},description:""},ariaLabel:{required:!0,tsType:{name:"string"},description:""},className:{required:!1,tsType:{name:"string"},description:""},as:{required:!1,tsType:{name:"union",raw:'"button" | "span"',elements:[{name:"literal",value:'"button"'},{name:"literal",value:'"span"'}]},description:'Render the trigger as a real `<button>` (default) or as a\n`role="button"` `<span>`. Use `"span"` when composing this inside\nanother real `<button>` — e.g. `Tab`\'s `menuItems` slot — since HTML\nforbids nesting interactive controls inside a `<button>` (the browser\nwill otherwise silently mis-parse the DOM). Same reasoning as\n`InteractionNavItem`\'s own `role="button"` outer `<div>` and `Tab`\'s\nown `onRemove` span. Behavior (positioning, keyboard, outside click) is\nidentical either way — `"span"` just adds `tabIndex={0}` and an\nEnter/Space key handler to stand in for the button semantics browsers\ngive `<button>` for free.',defaultValue:{value:'"button"',computed:!1}},icon:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:"Trigger icon — defaults to the vertical kebab (⋮). Pass a different\n icon (e.g. `MoreHorizontal`) for callers that need the same\n button+dropdown behavior with a different glyph, like\n `BreadcrumbEllipsis` (see breadcrumb.tsx)."},align:{required:!1,tsType:{name:"union",raw:'"left" | "right"',elements:[{name:"literal",value:'"left"'},{name:"literal",value:'"right"'}]},description:`Which edge of the trigger the dropdown's own edge aligns to (default:
\`"right"\`). \`"right"\` — the dropdown's right edge aligns with the
trigger's right edge, opening to the left — is correct for a trigger
that sits at the *end* of a row (a card header, a table row) where
opening rightward would run off the viewport. \`"left"\` — the
dropdown's left edge aligns with the trigger's left edge, opening to
the right — is for triggers nearer the left edge of the screen, e.g.
\`BreadcrumbEllipsis\` mid-trail; \`"right"\` there runs the dropdown off
the left of the viewport instead. Maps directly to Radix's own
\`align="end"\`/\`"start"\`.`,defaultValue:{value:'"right"',computed:!1}},onOpenChange:{required:!1,tsType:{name:"signature",type:"function",raw:"(open: boolean) => void",signature:{arguments:[{type:{name:"boolean"},name:"open"}],return:{name:"void"}}},description:"Fires whenever the dropdown opens or closes. Lets a caller that also\nwraps this trigger in its own `Tooltip` (e.g. `Tab`'s `menuItems` slot,\nor a card header's kebab) know when the dropdown is showing, so it can\npass that straight to `Tooltip`'s own `disabled` prop and force the\ntooltip closed for as long as the dropdown is open. Without this, the\ntwo components have no way to coordinate: the tooltip's trigger and the\ndropdown's trigger are the same DOM node, so nothing tells the tooltip\nto stay shut once the dropdown takes over that same area — see\nCONTRIBUTING.md's \"Tooltip wrapping a nested Menu trigger\" note."},badge:{required:!1,tsType:{name:"number"},description:'Count badge overlaid on the trigger\'s top-right corner — hidden when 0\nor undefined. Same shared idiom `Button`\'s own `badge` prop uses\n(button.tsx: a `relative inline-flex` wrapper around the icon plus a\n`Badge shape="circle" variant="critical" size="sm"` positioned\n`absolute -top-2 -right-2`), reproduced here rather than composing\n`Button` itself — this trigger isn\'t built on `Button` (see the class\ncomment above: it\'s its own hand-rolled `<button>`/`<span>` so it can\nrender as either). First added for "View All Apps" (`AgentNextGenPage\n.tsx`) surfacing an unread-notifications count once Notifications\nitself is unpinned from the header (and so no longer showing its own\n`NotificationsBell` badge) — without this, unpinning Notifications\nwould silently hide the only visual cue that new notifications exist.'},disabled:{required:!1,tsType:{name:"boolean"},description:"Locks the trigger — no dropdown opens, dimmed/`cursor-not-allowed`\nstyling matching `Button`'s own `disabled` look, rather than removing\nthe kebab outright. First added for a caller that wants this exact\n\"visible but locked\" treatment for a temporary state (e.g. a session\nunder active review) as opposed to a permanent one (a closed session\nalready just omits this component entirely rather than rendering it\ndisabled — see `TranscriptSessionSeparator`'s own `isClosed` gate,\nagent-next-gen-transcript.tsx)."},open:{required:!1,tsType:{name:"boolean"},description:"Controlled open state, passed straight to `MenuRadix`. Lets a parent\n open the menu from its own keyboard shortcut — e.g. `Tab` opens its\n kebab on Shift+F10, since that kebab is deliberately not focusable\n inside the tab (see tabs.tsx). Omit for normal uncontrolled use."}},composes:["Omit"]};export{c as K};
