import{j as n}from"./jsx-runtime-D_zvdyIk.js";import{r as u}from"./index-CXOcBcs0.js";import{L as g}from"./left-nav-BFcm0xIS.js";import{O as S,u as ce,C as de}from"./create-new-outbound-mock-BGUiKDGp.js";import{I as le}from"./interaction-nav-item-BA7uGvQ8.js";import{C as he}from"./container-D7iDnn4B.js";import{C as pe}from"./content-area-sNeaqXFh.js";import{H as me}from"./house-7dsFQekL.js";import{S as ue}from"./search-aUstRSOi.js";import{c as X}from"./createLucideIcon-DEcfmm_F.js";import{C as ge}from"./circle-help-Bj2MpUE2.js";import{S as A}from"./settings-Ddbozet5.js";import{M as fe}from"./monitor-DREitwVn.js";import{L as Y}from"./layout-grid-DIlLALBe.js";import{F as j}from"./file-text-D-AW36xm.js";import{G as ve}from"./gauge-CgEhWZVI.js";import{C as be}from"./chart-column-BD5V0Ndt.js";import"./_commonjsHelpers-CqkleIqs.js";import"./tree-menu-I7AyROaY.js";import"./utils-BLSKlp9E.js";import"./chevron-down-BRCsRsv-.js";import"./tooltip-DsDWII6n.js";import"./index-DNfP5j1O.js";import"./index-DWcjTdiE.js";import"./index-DujEZ9g2.js";import"./chevron-left-C6DiQdwt.js";import"./chevron-right-DZKRY3zX.js";import"./menu-C3iBPI2b.js";import"./input-BgypaUyl.js";import"./error-icon-DM5nl_7y.js";import"./label-DRpt0Xe7.js";import"./popover-B7lHmnGD.js";import"./index-Cfrq8Ckk.js";import"./Combination-BD090wx7.js";import"./tslib.es6-Ytcc2UEA.js";import"./container-header-Bfsb3MJm.js";import"./x-N8aIqrq2.js";import"./select-CzSY58Al.js";import"./checkbox-BbegBx1f.js";import"./index-CoT6TaLL.js";import"./minus-DYrWPnXn.js";import"./check-DrRFj5bn.js";import"./button-5FlDPGRL.js";import"./index-BDkVnVO1.js";import"./index-1evVQkiP.js";import"./table-f6fiU3Iq.js";import"./search-input-BPFwehGD.js";import"./filter-chip-C3l-TRr7.js";import"./ellipsis-vertical-CZvSBcNM.js";import"./panel-left-CWVFPQ0g.js";import"./panel-right-CgZ2ABSM.js";import"./arrow-up-C-teBDU4.js";import"./favorite-button-DNbJO1Tv.js";import"./star-BBKukw_S.js";import"./phone-input-Df6Apnf-.js";import"./tag-vFMi8jZv.js";import"./plus-B2SVJpWV.js";import"./phone-Di4N1bEU.js";import"./mail-CGsQAUqz.js";import"./message-square-BpbTPZlK.js";import"./channel-row-gFdTDUzZ.js";import"./kebab-menu-button-BG6m3PIv.js";import"./tabs-QuTI4Gjp.js";import"./clock-xCVatdV-.js";import"./triangle-alert-Btkn3DL5.js";import"./circle-check-Bqo3g0Bw.js";import"./user-rDz6zf5M.js";/**
 * @license lucide-react v0.468.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */const we=X("NotebookPen",[["path",{d:"M13.4 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-7.4",key:"re6nr2"}],["path",{d:"M2 6h4",key:"aawbzj"}],["path",{d:"M2 10h4",key:"l0bgd4"}],["path",{d:"M2 14h4",key:"1gsvsf"}],["path",{d:"M2 18h4",key:"1bu2t1"}],["path",{d:"M21.378 5.626a1 1 0 1 0-3.004-3.004l-5.01 5.012a2 2 0 0 0-.506.854l-.837 2.87a.5.5 0 0 0 .62.62l2.87-.837a2 2 0 0 0 .854-.506z",key:"pqwjuv"}]]);/**
 * @license lucide-react v0.468.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */const ye=X("PencilRuler",[["path",{d:"M13 7 8.7 2.7a2.41 2.41 0 0 0-3.4 0L2.7 5.3a2.41 2.41 0 0 0 0 3.4L7 13",key:"orapub"}],["path",{d:"m8 6 2-2",key:"115y1s"}],["path",{d:"m18 16 2-2",key:"ee94s4"}],["path",{d:"m17 11 4.3 4.3c.94.94.94 2.46 0 3.4l-2.6 2.6c-.94.94-2.46.94-3.4 0L11 17",key:"cfq27r"}],["path",{d:"M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z",key:"1a8usu"}],["path",{d:"m15 5 4 4",key:"1mk7zo"}]]),D=[{icon:n.jsx(fe,{className:"h-4 w-4",strokeWidth:1.5}),label:"Monitor"},{icon:n.jsx(Y,{className:"h-4 w-4",strokeWidth:1.5}),label:"Dashboard"},{icon:n.jsx(A,{className:"h-4 w-4",strokeWidth:1.5}),label:"Configure",expandable:!0,defaultOpen:!1,children:[{label:"General"},{label:"Security"},{label:"Integrations"}]},{icon:n.jsx(ye,{className:"h-4 w-4",strokeWidth:1.5}),label:"Designer",expandable:!0,defaultOpen:!0,children:[{label:"Desktop Library",active:!0},{label:"Templates"},{label:"Components"}]},{icon:n.jsx(j,{className:"h-4 w-4",strokeWidth:1.5}),label:"Examples"},{icon:n.jsx(j,{className:"h-4 w-4",strokeWidth:1.5}),label:"Product Mockups"}],Rn={title:"UI/LeftNav",component:g,tags:["autodocs"],parameters:{layout:"fullscreen"},decorators:[a=>n.jsxs("div",{className:"flex h-screen bg-lyra-bg-surface-shell",children:[n.jsx(a,{}),n.jsx(pe,{children:n.jsx(he,{className:"flex flex-1"})})]})]},y={render:()=>{const[a,r]=u.useState(!0);return n.jsx(g,{items:D,open:a,onToggle:()=>r(d=>!d)})}},x={name:"Collapsed",render:()=>{const[a,r]=u.useState(!1);return n.jsx(g,{items:D,open:a,onToggle:()=>r(d=>!d)})}},N={name:"No Toggle Button",render:()=>n.jsx(g,{items:D,open:!0,collapsible:!1})},k={name:"Agent Next Gen Left Nav",render:()=>{const[a,r]=u.useState(!1),[d,h]=u.useState(""),[O,f]=u.useState([]),Z=e=>{var o;const t=(o=S.skillOptions.find(c=>c.value===e.skillId))==null?void 0:o.label,s={id:`${e.channel}:${e.phone}`,type:e.channel,elapsed:"00:00",current:!0,preview:t};f(c=>{const l=c.findIndex(i=>i.id===e.contact.id);return l===-1?[...c,{id:e.contact.id,customerName:e.contact.name,elapsed:"00:00",channels:[s],channelAddresses:{[e.channel]:[e.phone]}}]:c.map((i,p)=>{var W;if(p!==l)return i;const v=i.channels.findIndex(w=>(w.id??w.type)===s.id),b=v===-1?[...i.channels,s]:i.channels.map((w,re)=>re===v?s:w),m=((W=i.channelAddresses)==null?void 0:W[e.channel])??[];return{...i,elapsed:"00:00",channels:b,channelAddresses:{...i.channelAddresses,[e.channel]:m.includes(e.phone)?m:[...m,e.phone]}}})}),h(e.contact.id),r(!0)},ee=e=>{const t=`quickdial:${e}`,s={id:"voice",type:"voice",elapsed:"00:00",current:!0};f(o=>{const c=o.findIndex(l=>l.id===t);return c===-1?[...o,{id:t,elapsed:"00:00",channels:[s]}]:o.map((l,i)=>i===c?{...l,elapsed:"00:00",channels:[s]}:l)}),h(t),r(!0)},ne=e=>{f(t=>t.filter(s=>s.id!==e)),h(t=>t===e?"":t)},te=(e,t)=>{var l;const s=t.id??t.type,o=`${t.type}:`,c=(l=t.id)!=null&&l.startsWith(o)?t.id.slice(o.length):void 0;f(i=>i.map(p=>{var b;if(p.id!==e)return p;const v=c?{...p.channelAddresses,[t.type]:(((b=p.channelAddresses)==null?void 0:b[t.type])??[]).filter(m=>m!==c)}:p.channelAddresses;return{...p,channels:p.channels.filter(m=>(m.id??m.type)!==s),channelAddresses:v}}))},T=u.useMemo(()=>{const e=new Map(O.filter(t=>t.channelAddresses).map(t=>[t.id,t.channelAddresses]));return{...S,groups:S.groups.map(t=>t.contacts?{...t,contacts:t.contacts.map(s=>{const o=e.get(s.id);return!o||Object.keys(o).length===0?s:{...s,openChannelAddresses:o}})}:t)}},[O]),{launchRequest:se,onLaunchRequestHandled:ae,getHeaderAction:oe}=ce(T),ie=[{icon:n.jsx(me,{className:"h-4 w-4",strokeWidth:1.5}),label:"Desk",active:d==="",onClick:()=>h("")},{icon:n.jsx(ue,{className:"h-4 w-4",strokeWidth:1.5}),label:"Search"},{icon:n.jsx(we,{className:"h-4 w-4",strokeWidth:1.5}),label:"WEM"},{icon:n.jsx(ge,{className:"h-4 w-4",strokeWidth:1.5}),label:"Help"},{icon:n.jsx(A,{className:"h-4 w-4",strokeWidth:1.5}),label:"Settings"}];return n.jsx(g,{items:ie,open:a,onToggle:()=>r(e=>!e),pinnedHeader:n.jsx(de,{title:"New Outbound",outbound:{...T,onStartCall:Z,onQuickDial:ee,launchRequest:se,onLaunchRequestHandled:ae},expanded:a}),header:n.jsx(n.Fragment,{children:O.map(e=>n.jsx(le,{customerName:e.customerName,active:d===e.id,onClick:()=>h(e.id),awaitingResponse:e.awaitingResponse,elapsed:e.elapsed,expanded:a,channels:e.channels,onDismiss:()=>ne(e.id),onDismissChannel:t=>te(e.id,t),headerAction:oe(e.id)},e.id))})})}},C={name:"Outbound Engagement Left Nav",render:()=>{const[a,r]=u.useState(!0),d=[{icon:n.jsx(ve,{className:"h-4 w-4",strokeWidth:1.5}),label:"Monitor",active:!0},{icon:n.jsx(A,{className:"h-4 w-4",strokeWidth:1.5}),label:"Configure"},{icon:n.jsx(be,{className:"h-4 w-4",strokeWidth:1.5}),label:"Review"}];return n.jsx(g,{items:d,open:a,onToggle:()=>r(h=>!h)})}},I={name:"Custom Items",render:()=>{const[a,r]=u.useState(!0),d=[{icon:n.jsx(Y,{className:"h-4 w-4",strokeWidth:1.5}),label:"Overview",active:!0},{icon:n.jsx(A,{className:"h-4 w-4",strokeWidth:1.5}),label:"Settings",expandable:!0,defaultOpen:!0,children:[{label:"Profile"},{label:"Notifications",active:!0},{label:"Privacy"}]},{icon:n.jsx(j,{className:"h-4 w-4",strokeWidth:1.5}),label:"Reports"}];return n.jsx(g,{items:d,open:a,onToggle:()=>r(h=>!h)})}};var L,M,R;y.parameters={...y.parameters,docs:{...(L=y.parameters)==null?void 0:L.docs,source:{originalSource:`{
  render: () => {
    const [open, setOpen] = useState(true);
    return <LeftNav items={sampleItems} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(R=(M=y.parameters)==null?void 0:M.docs)==null?void 0:R.source}}};var G,H,P;x.parameters={...x.parameters,docs:{...(G=x.parameters)==null?void 0:G.docs,source:{originalSource:`{
  name: "Collapsed",
  render: () => {
    const [open, setOpen] = useState(false);
    return <LeftNav items={sampleItems} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(P=(H=x.parameters)==null?void 0:H.docs)==null?void 0:P.source}}};var B,q,E;N.parameters={...N.parameters,docs:{...(B=N.parameters)==null?void 0:B.docs,source:{originalSource:`{
  name: "No Toggle Button",
  render: () => <LeftNav items={sampleItems} open={true} collapsible={false} />
}`,...(E=(q=N.parameters)==null?void 0:q.docs)==null?void 0:E.source}}};var U,F,$;k.parameters={...k.parameters,docs:{...(U=k.parameters)==null?void 0:U.docs,source:{originalSource:`{
  name: "Agent Next Gen Left Nav",
  render: () => {
    const [open, setOpen] = useState(false);
    // Click any InteractionNavItem card below to make it the active one —
    // lets you test the active/inactive + awaiting-response border and
    // highlight states interactively instead of only via fixed args.
    const [activeId, setActiveId] = useState("");

    // Empty until the agent actually starts an interaction from CreateNew's
    // "New Outbound" flow (see handleStartCall/handleQuickDial) — same
    // "no cards until launched" behavior as \`agent-next-gen-v1\` and
    // \`AgentNextGenTemplate.stories.tsx\` (which never seeded fixed demo
    // cards to begin with). This story used to seed 3 fixed placeholder
    // cards ("Sofia Martinez"/"Ray Torres"/"Customer") with ids that had no
    // matching contact in \`OUTBOUND_CONFIG\` — clicking their "+" could never
    // actually open CreateNew's call-setup screen, which looked like a bug
    // in the "+" button itself rather than what it actually was (cards with
    // no real contact behind them). Starting empty and only populating via
    // a real "Start Interaction" click means every card that appears here
    // is backed by a real contact, so its own "+" button always works.
    const [interactions, setInteractions] = useState<AgentNextGenDemoInteraction[]>([]);
    // Starting a new outbound call/quick dial adds (or restarts) a card —
    // and, since a collapsed nav would otherwise hide that new card from
    // view entirely, also expands the nav so the agent immediately sees
    // what they just started instead of having to notice and open it
    // themselves.
    const handleStartCall = (selection: {
      contact: CreateNewOutboundContact;
      channel: ChannelType;
      phone: string;
      skillId: string;
    }) => {
      // Body copy shows the *actual* skill the agent picked in CreateNew's
      // outbound flow — not a random one — so the card reads as "here's
      // what this call was routed under," matching what the agent just
      // chose. \`selection.skillId\` is the value from \`OUTBOUND_CONFIG.
      // skillOptions\`; resolve it back to that option's own label.
      const skillLabel = OUTBOUND_CONFIG.skillOptions.find(o => o.value === selection.skillId)?.label;
      const newChannel: InteractionChannel = {
        id: \`\${selection.channel}:\${selection.phone}\`,
        type: selection.channel,
        elapsed: "00:00",
        current: true,
        preview: skillLabel
      };
      setInteractions(prev => {
        const idx = prev.findIndex(i => i.id === selection.contact.id);
        // No existing card for this contact — start a new one.
        if (idx === -1) {
          return [...prev, {
            id: selection.contact.id,
            customerName: selection.contact.name,
            elapsed: "00:00",
            channels: [newChannel],
            channelAddresses: {
              [selection.channel]: [selection.phone]
            }
          }];
        }
        // Same contact already has a card open — restart the matching
        // channel if this is the *same* type+address (e.g. redialing the
        // same SMS number), or add a new row alongside the existing ones if
        // it's a different address on the same type (e.g. a second SMS
        // thread on a different number) — those are genuinely separate
        // conversations, not a duplicate of the first, so they shouldn't
        // overwrite it.
        return prev.map((interaction, i) => {
          if (i !== idx) return interaction;
          const chIdx = interaction.channels.findIndex(c => (c.id ?? c.type) === newChannel.id);
          const channels = chIdx === -1 ? [...interaction.channels, newChannel] : interaction.channels.map((c, j) => j === chIdx ? newChannel : c);
          const existingAddresses = interaction.channelAddresses?.[selection.channel] ?? [];
          return {
            ...interaction,
            elapsed: "00:00",
            channels,
            channelAddresses: {
              ...interaction.channelAddresses,
              [selection.channel]: existingAddresses.includes(selection.phone) ? existingAddresses : [...existingAddresses, selection.phone]
            }
          };
        });
      });
      setActiveId(selection.contact.id);
      setOpen(true);
    };
    const handleQuickDial = (phoneNumber: string) => {
      // No contact record for a quick-dialed number — key the card off the
      // number itself so redialing it restarts the existing card rather
      // than stacking up duplicates.
      const id = \`quickdial:\${phoneNumber}\`;
      const newChannel: InteractionChannel = {
        id: "voice",
        type: "voice",
        elapsed: "00:00",
        current: true
      };
      setInteractions(prev => {
        const idx = prev.findIndex(i => i.id === id);
        if (idx === -1) return [...prev, {
          id,
          elapsed: "00:00",
          channels: [newChannel]
        }];
        return prev.map((interaction, i) => i === idx ? {
          ...interaction,
          elapsed: "00:00",
          channels: [newChannel]
        } : interaction);
      });
      setActiveId(id);
      setOpen(true);
    };

    // "Unassign & Dismiss" — InteractionNavItem itself decides which of
    // these two applies (based on how many channels the card has open when
    // it's clicked): \`onDismiss\` (whole card, only called when just one
    // channel was open) removes the interaction entirely, clearing
    // \`activeId\` too if it was the active one, same as the live app's
    // \`handleDismissInteraction\`; \`onDismissChannel\` (only called when more
    // than one channel was open) drops just that one channel.
    const handleDismissInteraction = (id: string) => {
      setInteractions(prev => prev.filter(interaction => interaction.id !== id));
      setActiveId(current => current === id ? "" : current);
    };
    const handleDismissChannel = (id: string, channel: InteractionChannel) => {
      // Match on \`id\` (falling back to \`type\`, same as InteractionNavItem's
      // own \`channelKey\` convention) — two open channels can share a \`type\`
      // (e.g. two SMS threads on different numbers), so filtering by \`type\`
      // alone would drop *both* instead of just the one dismissed.
      const dismissedKey = channel.id ?? channel.type;
      // \`id\` is built as \`\${type}:\${address}\` in handleStartCall — recover
      // the address so only *that* entry (not every address for this type)
      // is dropped from \`channelAddresses\` too, otherwise a still-open
      // second SMS thread would wrongly stop disabling its own address in
      // CreateNew once this one is dismissed.
      const addressPrefix = \`\${channel.type}:\`;
      const dismissedAddress = channel.id?.startsWith(addressPrefix) ? channel.id.slice(addressPrefix.length) : undefined;
      setInteractions(prev => prev.map(interaction => {
        if (interaction.id !== id) return interaction;
        const remainingAddresses = dismissedAddress ? {
          ...interaction.channelAddresses,
          [channel.type]: (interaction.channelAddresses?.[channel.type] ?? []).filter(a => a !== dismissedAddress)
        } : interaction.channelAddresses;
        return {
          ...interaction,
          channels: interaction.channels.filter(c => (c.id ?? c.type) !== dismissedKey),
          channelAddresses: remainingAddresses
        };
      }));
    };

    // A contact already reachable via a currently-open channel still shows
    // that channel in "Select Channel" and every address in the detail
    // screen's second field — except the one exact address already in use,
    // which is disabled. See AgentNextGenTemplate.stories.tsx's own copy of
    // this comment for the full rationale. Recomputed whenever
    // \`interactions\` changes so an address re-enables once its card is
    // dismissed.
    const outboundConfig = useMemo<CreateNewOutboundConfig>(() => {
      const openAddressesByContactId = new Map<string, Partial<Record<ChannelType, string[]>>>(interactions.filter(interaction => interaction.channelAddresses).map(interaction => [interaction.id, interaction.channelAddresses as Partial<Record<ChannelType, string[]>>]));
      return {
        ...OUTBOUND_CONFIG,
        groups: OUTBOUND_CONFIG.groups.map(group => {
          if (!group.contacts) return group;
          return {
            ...group,
            contacts: group.contacts.map(contact => {
              const openChannelAddresses = openAddressesByContactId.get(contact.id);
              if (!openChannelAddresses || Object.keys(openChannelAddresses).length === 0) return contact;
              return {
                ...contact,
                openChannelAddresses
              };
            })
          };
        })
      };
    }, [interactions]);

    // Every "Agent Next Gen" consumer (this story, AgentNextGenTemplate.
    // stories.tsx, agent-next-gen-v1's AgentNextGenPage.tsx) wants the exact
    // same "+" behavior on each InteractionNavItem card — look up that
    // interaction's underlying outbound contact, scope the flyout to
    // whatever channels it actually supports (falling back to the full
    // unfiltered list for quick-dialed numbers/fixed demo cards with no
    // matching contact record), and deep-link a picked channel into
    // CreateNew's \`launchRequest\`. That's \`useOutboundAddButton\` (create-
    // new.tsx) — a single shared implementation instead of three hand-
    // copied ones that could (and did) quietly drift out of sync.
    const {
      launchRequest,
      onLaunchRequestHandled,
      getHeaderAction
    } = useOutboundAddButton(outboundConfig);

    // "Desk" is this page itself (see AgentDashboard/\`Templates/Dashboards\`'
    // PageHeader title) — active by default, and the only item here with no
    // fixed \`active\` flag: it's derived so that starting/selecting a real
    // interaction (activeId set) takes the active state away from Desk,
    // since focus has moved to that assignment instead. \`onClick\` clears
    // \`activeId\` back to "" so Desk stays a real way back to this page
    // instead of just a static "you're on the dashboard" indicator. Every
    // item below it is a plain static rail entry (per the reference
    // screenshot: Search, Directory, WEM, Help, Settings — Contacts/
    // Schedule dropped, Queue/Reporting intentionally not added here).
    const items: NavItem[] = [{
      icon: <Home className="h-4 w-4" strokeWidth={1.5} />,
      label: "Desk",
      active: activeId === "",
      onClick: () => setActiveId("")
    }, {
      icon: <Search className="h-4 w-4" strokeWidth={1.5} />,
      label: "Search"
    }, {
      icon: <NotebookPen className="h-4 w-4" strokeWidth={1.5} />,
      label: "WEM"
    }, {
      icon: <HelpCircle className="h-4 w-4" strokeWidth={1.5} />,
      label: "Help"
    }, {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Settings"
    }];
    return <LeftNav items={items} open={open} onToggle={() => setOpen(v => !v)} pinnedHeader={<CreateNew title="New Outbound" outbound={{
      ...outboundConfig,
      onStartCall: handleStartCall,
      onQuickDial: handleQuickDial,
      launchRequest,
      onLaunchRequestHandled
    }} expanded={open} />} header={<>
            {interactions.map(interaction => <InteractionNavItem key={interaction.id} customerName={interaction.customerName} active={activeId === interaction.id} onClick={() => setActiveId(interaction.id)} awaitingResponse={interaction.awaitingResponse} elapsed={interaction.elapsed} expanded={open} channels={interaction.channels} onDismiss={() => handleDismissInteraction(interaction.id)} onDismissChannel={channel => handleDismissChannel(interaction.id, channel)} headerAction={getHeaderAction(interaction.id)} />)}
          </>} />;
  }
}`,...($=(F=k.parameters)==null?void 0:F.docs)==null?void 0:$.source}}};var _,Q,z;C.parameters={...C.parameters,docs:{...(_=C.parameters)==null?void 0:_.docs,source:{originalSource:`{
  name: "Outbound Engagement Left Nav",
  render: () => {
    const [open, setOpen] = useState(true);
    const items: NavItem[] = [{
      icon: <Gauge className="h-4 w-4" strokeWidth={1.5} />,
      label: "Monitor",
      active: true
    }, {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Configure"
    }, {
      icon: <BarChart3 className="h-4 w-4" strokeWidth={1.5} />,
      label: "Review"
    }];
    return <LeftNav items={items} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(z=(Q=C.parameters)==null?void 0:Q.docs)==null?void 0:z.source}}};var K,J,V;I.parameters={...I.parameters,docs:{...(K=I.parameters)==null?void 0:K.docs,source:{originalSource:`{
  name: "Custom Items",
  render: () => {
    const [open, setOpen] = useState(true);
    const items: NavItem[] = [{
      icon: <LayoutGrid className="h-4 w-4" strokeWidth={1.5} />,
      label: "Overview",
      active: true
    }, {
      icon: <Settings className="h-4 w-4" strokeWidth={1.5} />,
      label: "Settings",
      expandable: true,
      defaultOpen: true,
      children: [{
        label: "Profile"
      }, {
        label: "Notifications",
        active: true
      }, {
        label: "Privacy"
      }]
    }, {
      icon: <FileText className="h-4 w-4" strokeWidth={1.5} />,
      label: "Reports"
    }];
    return <LeftNav items={items} open={open} onToggle={() => setOpen(v => !v)} />;
  }
}`,...(V=(J=I.parameters)==null?void 0:J.docs)==null?void 0:V.source}}};const Gn=["Default","Collapsed","NoToggle","AgentNextGen","OutboundEngagement","CustomItems"];export{k as AgentNextGen,x as Collapsed,I as CustomItems,y as Default,N as NoToggle,C as OutboundEngagement,Gn as __namedExportsOrder,Rn as default};
