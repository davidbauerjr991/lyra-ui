import{j as e}from"./jsx-runtime-D_zvdyIk.js";import{r as N}from"./index-DhMLlvMY.js";import{T as c}from"./tooltip-B7WaxDQZ.js";import{c as n}from"./utils-BLSKlp9E.js";import{c as k}from"./createLucideIcon-aII_sYFw.js";import{X as R}from"./x-CzxgOx-T.js";/**
 * @license lucide-react v0.468.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */const T=k("ArrowLeft",[["path",{d:"m12 19-7-7 7-7",key:"1l729n"}],["path",{d:"M19 12H5",key:"x3x0zl"}]]),h=N.forwardRef(({className:u,title:t,icon:s,onBack:o,backIcon:p=e.jsx(T,{className:"h-5 w-5",strokeWidth:1.5,"aria-hidden":"true"}),actions:f,onClose:i,closeIcon:m=e.jsx(R,{className:"h-5 w-5",strokeWidth:1.5,"aria-hidden":"true"}),titleClassName:b="lyra-heading-md",subhead:r,titleBadge:l,topSlot:d,tabs:a,bordered:w=!0,background:y="none",actionsWrap:g=!1,...v},x)=>e.jsxs("div",{ref:x,className:n("flex flex-col shrink-0",w&&!a&&"border-b border-lyra-border-subtle",y==="subtle"&&"bg-lyra-bg-control-subtle",g&&"lyra-container-header-actions-wrap"),...v,children:[e.jsxs("div",{className:n("flex items-center justify-between px-4",a?"pt-2.5":"py-2.5",u),children:[e.jsxs("div",{className:"flex flex-1 flex-col gap-1 min-w-0",children:[d&&e.jsx("div",{className:"self-start",children:d}),e.jsxs("div",{className:"flex items-center gap-2 min-w-0",children:[o&&e.jsx(c,{content:"Back",placement:"bottom",asLabel:!0,children:e.jsx("button",{"aria-label":"Back",onClick:o,className:"flex h-8 w-8 shrink-0 items-center justify-center rounded-lyra-sm text-lyra-fg-action hover:bg-lyra-state-hover transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-lyra-border-focus focus-visible:ring-offset-2",children:p})}),s&&e.jsx("span",{className:"flex-shrink-0 text-lyra-fg-secondary",children:s}),e.jsx("div",{className:"min-w-0 flex-1",children:(t||l)&&e.jsxs("div",{className:"flex min-h-10 flex-col justify-center overflow-hidden min-w-0",children:[e.jsxs("div",{className:"flex items-center gap-2 min-w-0",children:[t&&e.jsx("h2",{className:n(b,"text-lyra-fg-default truncate min-w-0"),children:t}),l]}),r&&e.jsx("p",{className:"lyra-body-sm text-lyra-fg-secondary truncate min-w-0",children:r})]})})]})]}),e.jsxs("div",{className:"flex items-center gap-1 shrink-0 ml-4",children:[f,i&&e.jsx(c,{content:t?`Close ${t}`:"Close",placement:"bottom",asLabel:!0,children:e.jsx("button",{"aria-label":t?`Close ${t}`:"Close",onClick:i,className:"flex h-8 w-8 items-center justify-center rounded-lyra-sm text-lyra-fg-action hover:bg-lyra-state-hover transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-lyra-border-focus focus-visible:ring-offset-2",children:m})})]})]}),a]}));h.displayName="ContainerHeader";h.__docgenInfo={description:"",methods:[],displayName:"ContainerHeader",props:{title:{required:!1,tsType:{name:"string"},description:`Header title. Optional so the header can still render for icon-only /
tag-only layouts (e.g. \`DashboardCard\`'s "show/hide header text"
control) — when omitted, the title line simply isn't rendered rather
than showing an empty heading.`},icon:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:"Optional icon to the left of the title"},onBack:{required:!1,tsType:{name:"signature",type:"function",raw:"() => void",signature:{arguments:[],return:{name:"void"}}},description:`Renders a real back-arrow button to the left of \`icon\`/the title
(mirroring \`onClose\`'s own button — same size/hover/focus treatment,
just on the leading edge) and calls this when clicked. For panel
content that drills into a sub-view within the SAME panel (rather
than opening a second, nested one — this design system's panels
don't support nesting) and needs a way back out. Omit for a header
with no drill-down navigation of its own (every existing consumer is
unaffected).`},backIcon:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:"Icon rendered inside the back button (only meaningful when `onBack`\n is set). Default: lucide `ArrowLeft`.",defaultValue:{value:'<ArrowLeft className="h-5 w-5" strokeWidth={1.5} aria-hidden="true" />',computed:!1}},actions:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:"Actions rendered to the right of the title (buttons, badges, etc.)"},onClose:{required:!1,tsType:{name:"signature",type:"function",raw:"() => void",signature:{arguments:[],return:{name:"void"}}},description:"Renders a built-in close button and calls this when clicked"},closeIcon:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:'Icon rendered inside the built-in close button (only meaningful when\n`onClose` is set). Default: lucide `X`, unchanged for every existing\nconsumer. Lets a caller swap in a more specific glyph — e.g. `PanelRightClose`\nfor a panel that\'s genuinely being dismissed/closed as a docked panel,\nas opposed to a plain "X" which reads more like closing a tab/dialog.',defaultValue:{value:'<X className="h-5 w-5" strokeWidth={1.5} aria-hidden="true" />',computed:!1}},titleClassName:{required:!1,tsType:{name:"string"},description:'Typography class for the title (default: "lyra-heading-md")',defaultValue:{value:'"lyra-heading-md"',computed:!1}},subhead:{required:!1,tsType:{name:"string"},description:"Optional subtitle displayed below the title in body-sm secondary"},titleBadge:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:"Badge or tag rendered inline immediately after the title"},topSlot:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:`Rendered on its own line above the icon+title row — e.g. a status
\`Tag\` ("Anomaly", "Alert") that needs to sit above the headline rather
than inline beside it like \`titleBadge\`. Left-aligned with the icon
(i.e. flush with the header's own left edge), not indented to the
title's start.`},bordered:{required:!1,tsType:{name:"boolean"},description:"Show a bottom border (default: true) — ignored (no border rendered\n here) whenever `tabs` is set, since the `TabList` passed there already\n supplies its own `border-b` as the header's true bottom edge; a second\n border directly above it would just double up.",defaultValue:{value:"true",computed:!1}},background:{required:!1,tsType:{name:"union",raw:'"none" | "subtle"',elements:[{name:"literal",value:'"none"'},{name:"literal",value:'"subtle"'}]},description:'Header background. `"subtle"` applies `bg-lyra-bg-control-subtle` (the\nsame subdued fill used by `FilterChip`\'s default variant) — useful when\nthe header needs to read as a distinct control/toolbar strip rather than\nblend into the surface behind it. Default `"none"` (transparent, inherits\nwhatever surface it\'s placed on).',defaultValue:{value:'"none"',computed:!1}},tabs:{required:!1,tsType:{name:"ReactReactNode",raw:"React.ReactNode"},description:"A `TabList` (or similar) rendered directly below the title/subhead row,\nstill inside this header — i.e. outside whatever scrolling body sits\nbelow the header (`PanelContent` in `SidePanel`/`InteriorPanel`, for\ninstance). Exists specifically so a panel's tabs can stay genuinely\nfixed without a hand-rolled `sticky` wrapper inside the scrollable body\n(see CONTRIBUTING.md's \"Composing panel body content\") — that approach\nstill scrolled with the surrounding container's own scrollbar and, in\n`agent-next-gen-v1`'s real usage, needed extra plumbing just to fake a\nfixed position. Passing `tabs` here is the real fix: the tab row simply\nisn't part of the scrolling region at all.\n\nWhen set, this header's own bottom padding is dropped (the tab row's\nown top edge sits flush against the title/subhead row above it) and\n`bordered` is ignored, since `TabList` already renders its own\n`border-b`. Pass the `TabList` element exactly as you would to any\nother consumer — including its own horizontal inset (`className=\"px-4\"`\nto match this header's own `px-4`) — this prop doesn't wrap or restyle\nit."},actionsWrap:{required:!1,tsType:{name:"boolean"},description:"Marks this header (`.lyra-container-header-actions-wrap`) as scoped\ninto the shared responsive-collapse family: a chip-style trigger in\n`actions` (e.g. `DateFilterChip` in agent-next-gen-v1) can shrink to a\ncompact icon via its own `-filter-full`/`-filter-compact` markup, and a\n`SearchInput` pair can swap which of two copies is visible via\n`-search-inline`/`-search-below` (one sitting in `actions`, the other\npassed through `tabs` so it renders on its own full-width row below the\ntitle) — see lyra-tokens.css's \"Container header actions\" families for\nthe concrete classes. Default `false`: a header with a couple of plain\nicon buttons in `actions` doesn't need any of this.\n\nOn its own this does nothing — it does NOT establish the CSS\ncontainer-query boundary those collapse rules react to. That's a\nSEPARATE, explicitly opt-in class, `.lyra-container-header-query-\nboundary` (`container-type: inline-size`), which the caller applies to\nwhichever ancestor's width should actually decide the collapse —\ntypically the card's own outer root, one level OUTSIDE this component,\nvia that card's own `className` (e.g. `DashboardCard`'s `className`).\nKept deliberately separate from this prop after a real regression: an\nearlier version had `actionsWrap` establish that boundary right here,\non this header's own row — every consumer that turned `actionsWrap` on\nshared the exact same width and threshold with no way to opt out, so\nraising `ContactHistoryCard`'s own 768px threshold silently broke\nnarrower dashboard widgets (Performance/Productivity's `DateFilterChip`)\nthat had only turned `actionsWrap` on for the icon-collapse fallback —\nthey went from \"always full\" to \"always collapsed\" with no code of\ntheir own changing. See `.lyra-container-header-query-boundary`'s own\ndoc comment (lyra-tokens.css) for the full story.\n\nDoes NOT wrap `actions` itself as a block (an earlier version of this\nprop did — `.lyra-container-header-actions-row`/`-actions` forcibly\ndropped the *whole* actions cluster to a new line, but that meant a\nlone filter chip with nothing else to make room for still got shoved\nonto its own row, and a `SearchInput` + `FilterChip` pair moved\ntogether as one unit even though only the search box actually needed\nto move — confirmed from a screenshot showing a lone `DateFilterChip`\noverlapping the title once forced onto its own full-width line for no\nreason). Prefer the per-element `-filter-*`/`-search-*` classes above\ninstead: each piece of `actions` decides its own collapse behavior,\nnothing moves that doesn't need to.",defaultValue:{value:"false",computed:!1}}}};export{h as C};
